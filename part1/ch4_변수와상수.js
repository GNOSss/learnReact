// 1. 변수
// 선언한다, 초기화한다
let sss = "seungsoo song";
console.log(sss);

sss = "coco kim";
console.log(sss);

let age;
console.log(age); // undefined

// let sss = "kimbora"; 중복 변수명을 사용 불가

// 2. 상수
const birth = 1993;
// birth = 1992; //상수는 선언 이후에는 값을 재할당 할 수 없음

/*
3. 변수 명명규칙(네이밍 규치)
- $, _ 제외한 기호는 사용할 수 없다. 
- 숫자로 시작할 수 없다. 
- 예약어로 사용할 수 없다. 
- 변수 명명 가이드
  - lowerCamelCase: myVariable
  - UPPER_CASE: MY_VARIABLE
  - snake_case: my_variable
  그 외에도 많지만 중요한건 가장 이해하기 쉬운 단어로 작성할 것
 */
